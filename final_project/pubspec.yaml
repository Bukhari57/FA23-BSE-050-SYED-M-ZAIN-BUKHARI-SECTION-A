
name: final_project
description: Smart POS & Inventory Management App (Firebase + SQLite)
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  sqflite: ^2.3.0
  path_provider: ^2.1.2
  provider: ^6.1.2
  intl: ^0.19.0
  googleapis: ^13.2.0
  google_sign_in: ^6.2.1

flutter:
  uses-material-design: true


import 'package:flutter/material.dart';

class LowStockAlertsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Low Stock Alerts'),
      ),
      body: Center(
        child: Text('Low Stock Alerts Screen'),
      ),
    );
  }
}
